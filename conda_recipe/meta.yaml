package:
  name: knotergy
  version: "0.2.2"

source:
  - path: ..

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake >=3.15
    - make
    - pkg-config
    - git

  host:
    - viennarna =2.7.2

  run:
    - viennarna =2.7.2

test:
  commands:
    - Knotergy --help

about:
  home: https://github.com/TheCOBRALab/Knotergy
  summary: "Knotergy is an algorithm for evaluating pseudoknotted secondary structures of RNA."
  description: |
    Knotergy evaluates RNA secondary structure.
  dev_url: https://github.com/TheCOBRALab/Knotergy

extra:
  recipe-maintainers:
    - PointlessUser